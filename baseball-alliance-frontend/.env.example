# Empty = use Vite dev server proxy to backend (see vite.config.ts → /api, /health → http://localhost:3000)
VITE_API_BASE_URL=

# Without proxy, point at the API origin (no trailing slash):
# VITE_API_BASE_URL=http://localhost:3000
