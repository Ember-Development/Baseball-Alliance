PORT=3000
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/baseball_alliance?schema=public"

# Required for POST /api/auth/login — use a long random string in production
JWT_SECRET="replace-with-a-long-random-secret"
JWT_EXPIRES_IN="7d"
